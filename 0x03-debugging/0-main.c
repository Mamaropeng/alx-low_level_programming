#include "main.h"

/**
 * main - entry point
 * Description: ' tests function that print if integer is positive or negative
 * Return: 0 (success)
 *
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
