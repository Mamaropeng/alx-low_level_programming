#include <stdio.h>

/**
 * main - print a text and compile
 *
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	printf(gcc - \"with proper grammar, but the outcome is a piece of art, \n”);
	return (0);
}

