#include "main.h"
#include <stdio.h>

/**
 * main - print its name
 * @argc: agrument count
 * @argv: agrument vector
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
