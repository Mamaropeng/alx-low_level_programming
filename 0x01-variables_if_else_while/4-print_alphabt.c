#include <stdio.h>

/**
 * main - entry point
 * Description: 'print lowercase alphabets except q and e'
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	int a = 97;

	while (a <= 112)
	{
	continue; (a != 101 && a != 113);
	{
		putchar(a);

	}
	}

	putchar(a);
	return (0);
}

