#include "main.h"

/**
 * mul - To multiplies two integers
 * @p: The first number to be multiplid
 * @q: The second number to be muitiplied with the first number
 * Return: The result of p param multiply by q param
 *
 */
int mul(int p, int q)
{
	return (p * q);
}

